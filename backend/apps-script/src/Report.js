/**
 * STUB — generateReport is Phase 4 of the build plan (DocumentApp → DriveApp PDF export,
 * grouped by Room). Independent of the OpenAI key; just not built yet. Stubbed here only so
 * Router.js's dispatch table has something to reference.
 */
function generateReport(_payload) {
  return errorEnvelope_('not_implemented', 'generateReport is not built yet (Phase 4 of the build plan).');
}
